<%= link_to gravatar_for(current_user, size: Settings.avatar_size_micropost), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("microposts.create.view_my_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, t("microposts.create.micropost")) %></span>
